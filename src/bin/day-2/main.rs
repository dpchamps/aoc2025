use anyhow::{Result, anyhow};
use aoc_2025::core::*;

fn problem_one(input: Vec<String>) -> Result<()> {
    Ok(())
}

fn problem_two(input: Vec<String>) -> Result<()> {
    Ok(())
}

fn main() -> Result<()> {
    let input = get_lines("day-2")?;
    run_problems(problem_one, problem_two, input)?;
    Ok(())
}
